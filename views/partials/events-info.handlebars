<article class="event">
      <div class="event_data">
        {{!-- post is correct, id is not --}}
        <a href="/post/{{id}}">{{event_name}}</a>
        <span>by {{username}} on {{event_date}} in {{event_category}}</span>
      </div>
      <div class="meta">
        {{location}} {{zip}} 
      </div>
</article>
